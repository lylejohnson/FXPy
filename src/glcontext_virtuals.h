public:
  // Overrides the base class version of create()
  virtual void create();

  // Calls the base class version of create()
  void _create();

  // Overrides the base class version of detach()
  virtual void detach();

  // Calls the base class version of detach()
  void _detach();

  // Overrides the base class version of destroy()
  virtual void destroy();

  // Calls the base class version of destroy()
  void _destroy();
